% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/search.R
\name{trials_fetch}
\alias{trials_fetch}
\title{Fetch clinical trial records from ClinicalTrials.gov}
\usage{
trials_fetch(
  query,
  max_records = 1000,
  .page_size = 1000,
  phase = NULL,
  country = NULL,
  from_date = "MIN",
  to_date = "MAX",
  date_filter = "LastUpdatePostDate",
  save_json = FALSE,
  json_file = NULL
)
}
\arguments{
\item{query}{Character string of search keywords.}

\item{max_records}{Maximum number of studies to retrieve.}

\item{.page_size}{Number of records per API request (internal).}

\item{phase}{Optional phase filter for the clinical trials.}

\item{country}{Optional country filter for the clinical trials.}

\item{from_date}{for filtering trials (YYYY-MM-DD or default "MIN")}

\item{to_date}{for filtering trials (YYYY-MM-DD or default "MAX").}

\item{date_filter}{Date field to filter on.
One of \code{"LastUpdatePostDate"} (default), \code{"StartDate"},
\code{"PrimaryCompletionDate"}, or \code{"CompletionDate"}.}

\item{save_json}{Logical; if \code{TRUE}, saves raw JSON to file instead of returning R objects.}

\item{json_file}{File path to save JSON output (if \code{save_json} is \code{TRUE}).}
}
\value{
A list with two elements:
\describe{
\item{studies}{A list of raw study records (JSON parsed as R lists).}
\item{meta}{Metadata describing the query and retrieval.}
}
}
\description{
Retrieves clinical trial records from the ClinicalTrials.gov v2 API
using keyword-based search and automatic pagination.
}
